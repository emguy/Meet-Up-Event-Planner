var React = require("react");

var EventCreatorContainer = React.createClass({

  render: function() {
    return (
      <div>
        New event form goes here.
      </div>
    );
  },
});

/* we only export the top most conponent */
module.exports = EventCreatorContainer;

